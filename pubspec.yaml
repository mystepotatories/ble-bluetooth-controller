name: robot_ble_controller
description: Bluetooth Serial controller for an ESP32 robot.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_analytics: ^12.5.0
  firebase_core: ^4.14.0
  flutter_bluetooth_serial:
    path: third_party/flutter_bluetooth_serial
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
